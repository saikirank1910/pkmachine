package com.sai.bestCodingPractices.two;

import java.util.Map;

public class CadApp {
	void drawShapes() {
	    Map<Integer, Shape> shapeMap= new TreeMap<Integer,Shape>();
		Shape shape = shapeMap.get(shape);
        shape.getType();
        shape.setShapeType();
        shape.getPoint();
        shape.setPoint();
	}
}
