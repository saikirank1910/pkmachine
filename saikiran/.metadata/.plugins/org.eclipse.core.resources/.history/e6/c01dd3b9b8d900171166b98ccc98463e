package com.sai.bestCodingPractices.six;

import java.util.*;

public class FoodDB{
	public Iterator traverseFoodList(String nameOfFood){

		TreeMap foodTree=new TreeMap();
		Iterator foodList;
		Food food;
		foodList = getAllRecords();
		while(foodList.hasNext()){
			food=(Food)foodList.next();
			//do its names contain both keywords?
			if((nameOfFood==null || nameOfFood.equals("") || food.getName().indexOf(nameOffood)!=-1))//nullor""meansnokeyisspecified
			{
				foodTree.put(food.getFoodKey(),food);
			}
		}
		return foodTree.values().iterator();
	}
	private Iterator getAllRecords()
	{
		return null;
	}
}

