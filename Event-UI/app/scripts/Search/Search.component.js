'use strict';

function searchController(appService, $location) {
  var ctrl = this;
  
}

angular.module('searchModule')
  .component('searchComponent', {
    templateUrl: '../../views/Search.html',
    controller: searchController,
  });